{
  "name": "jobbuddy-backend",
  "version": "1.0.0",
  "description": "Backend API for JobBuddy - AI-powered skill matcher for unemployed youth",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && npm install -g serve && npm run build:client",
    "build:client": "mkdir -p build && cp -r *.html css/ js/ build/"
  },
  "keywords": [
    "jobbuddy",
    "career",
    "skills",
    "resume",
    "job-matching",
    "api"
  ],
  "author": "JobBuddy Team",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.18.2",
    "html-pdf-node": "^1.0.8",
    "mongodb": "^6.18.0",
    "mongoose": "^8.17.0",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.54.1",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
